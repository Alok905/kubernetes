apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app-service: redis-service
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - port: 6379  # because apps are using 5432 port to connect to postgres
      targetPort: redis-port