apiVersion: v1
kind: Service
metadata:
  name: voting-service
  labels:
    app: voting-service
spec:
  type: NodePort
  selector:
    app: voting-app
  ports:
    - port: 5050
      targetPort: 80
      nodePort: 30004